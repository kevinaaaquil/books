# Backend API base URL (no trailing slash). Required when frontend is not served with backend on same origin.
# Examples: http://localhost:8080, https://api.example.com
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
